# Milestone 11: Live Crawl Semantic Acceptance

## Goal

Accept the user-authorized GoddessFantasy live crawl without exporting browser credentials and without allowing mechanically valid JSON to hide source-semantic loss or invented mechanics.

## Task 1: Preserve structured statline semantics (SEM-006)

- Add fixture-backed RED tests for fractional CR, the `暗蚀` necrotic alias, qualified known languages, and variable taxonomy.
- Classify rules as source-derived or corpus-derived; cover at least three positives where the rule generalizes, one close negative, and one unrelated Actor.
- Implement the narrow parser/model/generator changes needed to preserve schema-valid values and literal qualifiers.
- Regenerate affected v14 core Actors through the project workflow and compare generated fields with the live plaintext source.

## Task 2: Stop conditional-mechanics invention (SEM-007)

- Add RED tests proving replacement damage is not simultaneous while additive damage remains.
- Add RED tests proving multi-stage saving-throw outcomes are not linked as simultaneous immediate statuses, while simple immediate status applications still work.
- Implement clause-scoped, source-derived safe fallbacks. Preserve literal descriptions when a staged rule cannot be represented faithfully.
- Run structural checks and source-to-output semantic projections on Gremishka and Petrifying Death's Head plus unrelated controls.

## Task 3: Re-run and accept the live workflow (PROD-003)

- Re-run focused tests, production typecheck, `bun run audit:anti-overfit`, and the bounded full suite.
- Re-run the authenticated-session bridge and ordinary project pipeline without inspecting or exporting cookies.
- Verify manifest provenance, record/topic counts, plaintext output, generated Actor JSON, and representative simple/complex/multi-statblock samples using `docs/generated-actor-verification.md`.
- Update the parent ledger with mechanical evidence, semantic evidence, residual risk, and exact final state before Goal closure.
