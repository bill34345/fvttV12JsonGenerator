/**
 * Compatibility entrypoint.
 *
 * The CLI delivery application is owned by apps/cli. Keep this path while
 * existing scripts and operators migrate to the workspace app entrypoint.
 */
import '../apps/cli/src/main';
