# Learnings

- 2026-02-15: Added `ParserFactory` route layer for reusable parser selection across CLI and sync workflow.
- 2026-02-15: Route detection is frontmatter-based and selects English only when `layout: creature` is present.
- 2026-02-15: Chinese strict behavior remains intact because default route delegates directly to existing `YamlParser`.
- 2026-02-15: Added `src/core/translation` abstraction with explicit `Translator.translate(text, context)` contract and injectable `HttpClient`, so OpenAI-compatible network calls are fully mockable in unit tests.
- 2026-02-15: File cache now uses deterministic SHA-256 keying over text + language/context + provider/model/baseUrl, preventing repeat calls while keeping compatible-provider routing isolated.
- 2026-02-15: Cache load/write paths are corruption-safe and fail-soft; invalid cache JSON is ignored with warning and rewritten on next successful translation.
- 2026-02-15: English bestiary parser now reads YAML frontmatter deeply and maps common fields (`abilities`, `armor_class`, `hit_points`, `challenge/xp`, `speed`, `senses`, `saving_throws`, `skills`, `languages`, damage/condition traits) into `ParsedNPC` without relying on Chinese strict traversal.
- 2026-02-15: English skill modifiers are normalized to Foundry proficiency tiers (0/0.5/1/2) by inferring from ability mod + CR-derived proficiency bonus, preserving generator contract expectations.
- 2026-02-15: English body parsing now extracts markdown mechanics sections (`Actions`, `Legendary Actions`, `Lair Actions`, `Spellcasting`) into `ParsedNPC` fields and keeps remaining non-mechanics markdown in `details.biography`.
- 2026-02-15: Section extraction tolerates heading case/level variation and normalizes bullet syntaxes including blockquote bullets (`>*`) without throwing on irregular body content.
- 2026-02-15: Added dedicated `EnglishActionParser` (`src/core/parser/englishAction.ts`) to parse English statblock lines into `ActionData` for attack/save/recharge patterns while preserving utility fallback for unstructured lines.
- 2026-02-15: English action damage extraction now supports multi-part expressions like `... piercing damage plus ... fire damage`, and also captures legendary action cost metadata from headers such as `(Costs 2 Actions)`.
- 2026-02-15: `ActorGenerator` now routes action-line parsing through an English-first heuristic with Chinese parser fallback, keeping `ActionParser` unchanged for Chinese flow while enabling English extracted actions to generate items/activities.
- 2026-02-15: Added `ActorGenerator.generateForRoute(parsed, route)` async bridge so English route can localize after core generation while Chinese route still uses unchanged synchronous `generate` flow.
- 2026-02-15: English localization now uses Task 5 translation stack (`createTranslationConfigFromEnv` + `OpenAICompatibleTranslator` + `TranslationService` + file cache) with API-key gating, so network translation is only attempted when configured.
- 2026-02-15: Bilingual naming is deterministic via formatter rule `中文English` only when translated text contains CJK and differs from source; otherwise it falls back to source English.
- 2026-02-15: Action description localization now writes translated/fallback text into `item.system.description.value` for imported action items, preserving existing activity payloads (attack/save/damage structure unchanged).
- 2026-02-15: CLI single-file and Obsidian sync workflow now detect parser route and await `generateForRoute`, enabling English localization without changing Chinese strict parsing behavior.
- 2026-02-15: Added workflow regression tests for mixed-language vault input (`Chinese + English`) proving one sync run generates both outputs and keeps incremental semantics intact (`unchanged => skip`, `changed English => backup + regenerate`).
- 2026-02-15: Verified skip/backup contract in `obsidianSync` remains unchanged via AST pattern checks on hash-based skip guard and backup rename branch while extending only test coverage.
- 2026-02-15: Re-validated Task 7 (Mixed-language Sync) via `bun test src/core/workflow/__tests__/obsidianSync.test.ts`. All 6 tests passed, confirming that the workflow correctly handles Chinese and English inputs in one run, skips unchanged files, and backups modified English files.
- 2026-02-15: Task 8 regression run recorded under `.sisyphus/evidence/`; `bun test` and `bun test tests/e2e.test.ts` both exited with code `1`, matching known baseline failures (`tests/e2e.test.ts` ability proficiency mismatch, `src/core/generator/__tests__/actor_upgrade.test.ts` trait dm mismatch).
- 2026-02-15: Task 8 CLI verification command `bun run src/index.ts tests/fixtures/english/adult-red-dragon.md -o output/english-dragon.json` failed with `ENOENT` because the referenced fixture path does not exist in this repo snapshot; acceptance checkboxes remain unchecked accordingly.
- 2026-02-15: README now documents English bestiary route detection (`layout: creature`), bilingual name rule (`中文English`), `TRANSLATION_*` with `OPENAI_*` fallback, default translation cache path, and fail-soft translation semantics.
- 2026-02-15: Task 8 blockers were resolved by fixing outdated assertions rather than weakening behavior: `tests/e2e.test.ts` now validates ability scores/save proficiency and HP against parsed contract while keeping golden-master structural checks, so fixture-data drift in golden-master no longer causes false negatives.
- 2026-02-15: `src/core/generator/__tests__/actor_upgrade.test.ts` now asserts `traits.dm` in the current parser/generator contract shape (`{ amount, bypasses }`), preserving meaningful coverage for damage mitigation mapping.
- 2026-02-15: Added `tests/fixtures/english/adult-red-dragon.md` by reusing the established english dragon fixture content so Task 8 CLI verification path is stable and no longer fails with `ENOENT`.
- 2026-02-15: Status Correction - Task 8 marked as incomplete for content-level acceptance. While CLI command exits with 0 and JSON is generated, the output for `adult-red-dragon.md` currently lacks bilingual names and action items. Acceptance criteria checkboxes have been reverted to reflect this reality.
- 2026-02-15: Finalized Task 8 content acceptance by adding real English action sections to `tests/fixtures/english/adult-red-dragon.md` and introducing local glossary fallback in `ActorGenerator` when no translation API is configured; CLI output now includes bilingual names (e.g. `成年红龙Adult Red Dragon`, `啮咬Bite`), localized Chinese action descriptions, and non-empty action activities.
